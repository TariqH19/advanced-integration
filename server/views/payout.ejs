<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Multi-Recipient Payout</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f7f9fc;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        margin: 0;
        color: #333;
      }
      h1 {
        color: #333;
        text-align: center;
        margin-bottom: 20px;
      }
      #payout-form {
        background-color: #ffffff;
        border-radius: 8px;
        padding: 20px;
        width: 400px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      }
      .recipient {
        display: flex;
        gap: 10px;
        margin-bottom: 15px;
      }
      .recipient label {
        flex: 1;
        font-weight: bold;
      }
      input[type="email"],
      input[type="number"] {
        flex: 2;
        padding: 8px;
        border: 1px solid #ccc;
        border-radius: 4px;
        margin-top: 5px;
        width: 100%;
      }
      button {
        padding: 10px;
        font-size: 16px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        width: 100%;
        margin-top: 15px;
      }
      button[type="button"] {
        background-color: #007bff;
        color: #fff;
      }
      button[type="submit"] {
        background-color: #28a745;
        color: #fff;
      }
      #showPayoutButton {
        background-color: #ffc107;
        color: #fff;
        margin-top: 10px;
      }
      #payoutDetails {
        margin-top: 20px;
        font-size: 14px;
      }
    </style>
  </head>
  <body>
    <div>
      <h1>PayPal Payout to Multiple Recipients</h1>
      <form id="payout-form">
        <div id="recipients">
          <div class="recipient">
            <label>Email:</label>
            <input type="email" name="recipientEmail" required />
            <label>Amount (GBP):</label>
            <input
              type="number"
              name="recipientAmount"
              min="0.01"
              step="0.01"
              required />
          </div>
        </div>
        <button type="button" onclick="addRecipient()">
          Add Another Recipient
        </button>
        <button type="submit">Send Payout</button>
        <button id="showPayoutButton" onclick="showPayout()" disabled>
          Show Payout
        </button>
      </form>
      <div id="payoutDetails"></div>
    </div>

    <script src="payout.js"></script>
  </body>
</html>
